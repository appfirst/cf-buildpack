#!/usr/bin/env bash

# bin/compile <build-dir> <cache-dir>

# fail fast
set -e

# debug
# set -x

# parse and derive params
BUILD_DIR=$1
CACHE_DIR=$2

export BUILDPACK_PATH=$(dirname $(dirname $0))
source $BUILDPACK_PATH/bin/af_deploy --build-dir="$BUILD_DIR" --cache-dir="$CACHE_DIR"